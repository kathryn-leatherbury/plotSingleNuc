% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FeaturePlotMultiGene.R
\name{FeaturePlotMultiGene}
\alias{FeaturePlotMultiGene}
\title{Multi-gene UMAP feature overlay (Seurat)}
\usage{
FeaturePlotMultiGene(
  object,
  reduction = "umap",
  genes = NULL,
  na.cutoff = 0.5,
  max.expression = NULL,
  scale.legend = FALSE,
  scale.legend.max = FALSE,
  pt.size = 0.5,
  alpha = 0.5,
  group.by = NULL,
  label = TRUE,
  repel = FALSE,
  gene.ref.table = NULL,
  gene.ref.col = "mzebra_seurat_gene_id",
  new.name.col = "human_homolog",
  secondary.ref.col = "mzebra_to_human_true_homolog",
  convert.names = FALSE,
  assay = NULL,
  legend.ncols = NULL,
  plot.title = NULL,
  plot.subtitle = NULL
)
}
\arguments{
\item{object}{Seurat object.}

\item{reduction}{Character. Name of a 2D reduction (e.g., "umap", "umap.wnn").}

\item{genes}{Character vector of gene symbols to plot. May be mzebra IDs or
human symbols (mixed allowed) if a mapping table is provided.}

\item{na.cutoff}{Numeric. Lower limit; values <= this are treated as "off".}

\item{max.expression}{Optional numeric upper cap for scales (per gene if
\code{scale.legend = FALSE}, shared if \code{scale.legend = TRUE}).}

\item{scale.legend}{Logical. If TRUE, use a shared legend range for all genes.}

\item{scale.legend.max}{Logical. If TRUE and \code{scale.legend = TRUE}, use
the max of per-gene 95th percentiles; otherwise the min of per-gene 99th.}

\item{pt.size}{Numeric point size for colored points.}

\item{alpha}{Numeric alpha for colored points.}

\item{group.by}{NULL, a single column name in \code{object@meta.data}, or a
vector/factor of length \code{ncol(object)} used for cluster labels.}

\item{label}{Logical; draw text labels (cluster/group medoids).}

\item{repel}{Logical; if TRUE and ggrepel available, use repelled labels.}

\item{gene.ref.table}{Optional data.frame for symbol mapping.}

\item{gene.ref.col}{Column in \code{gene.ref.table} containing mzebra IDs.}

\item{new.name.col}{Column in \code{gene.ref.table} containing human symbols.}

\item{secondary.ref.col}{Logical column indicating "true homolog".}

\item{convert.names}{Logical; if TRUE and table provided, legend labels use
\code{new.name.col} (human) where available.}

\item{assay}{Character; if NULL uses \code{Seurat::DefaultAssay(object)}.}

\item{legend.ncols}{Optional integer; if provided, builds a multi-column
legend panel (requires cowplot + ggplotify).}

\item{plot.title}{Optional string for title.}

\item{plot.subtitle}{Optional string for subtitle.}
}
\value{
A ggplot object.
}
\description{
Draws a single UMAP (or other 2D reduction) and overlays multiple genes as
sequential colored layers with separate color scales (via ggnewscale).
Optionally maps mixed input symbols using a gene reference table, supports
shared or per-gene legends, and can place labels (with or without repel).
}
\examples{
\dontrun{
  FeaturePlotMultiGene(
    object = hypo,
    reduction = "umap.wnn",
    genes = c("csf1r", "LOC101479120", "cga", "fabp7", "myrf", "pitx2"),
    gene.ref.table = homologs,
    convert.names = TRUE,
    label = TRUE,
    repel = TRUE,
    legend.ncols = 2
  )
}
}
